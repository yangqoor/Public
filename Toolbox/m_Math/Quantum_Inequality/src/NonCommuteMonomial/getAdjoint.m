% Function: getAdjoint(mono)% Returns the Hermitian conjugate (particularly the conjugate transpose) of the monomial.% It is assumed that all variables in the monomial are Hermitian (or self-adjoint).% Methodology: reverses the ordering of variables in each partition%% Note that the adjoint of the monomial has the same degree and the ordering of variables is% simple and cannot be reduced further.function adjointMono = getAdjoint(mono)	% Initialize	adjointMono = NonCommuteMonomial();		adjointMono.m_degree = mono.m_degree;	adjointMono.m_coeff = mono.m_coeff;		numPartition = length(mono.m_varOrdering);	adjointMono.m_varOrdering = cell(1, numPartition);	% Note that A(end : -1 : 1) will reverse the array A	for index = 1 : numPartition		adjointMono.m_varOrdering{index} = mono.m_varOrdering{index}(end : -1 : 1);	endend