%--------------------------------------------------------------------------
%   新增滤波器函数，对列向量信号整体做滤波处理后输出
%--------------------------------------------------------------------------
function output = filter_w(w,sig)
w = w(:);
output = zeros(size(sig)+[length(w)-1 0]);
for idx = 1:size(sig,2)
    output(:,idx) = conv(w,sig(:,idx));
end