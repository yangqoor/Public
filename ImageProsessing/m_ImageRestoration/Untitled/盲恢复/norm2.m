function [b]=norm2(A,B)
%计算两矩阵的差的归一化矩阵2范数
%--------------------------------------
%按定义来作的
% C=A-B;
% C=C.^2;
% n=sum(C(:));
% m=length(C(:));
% c=sqrt(n/m);
%---------------------------
b=norm(A-B,'fro');
b=b/sqrt(numel(A));